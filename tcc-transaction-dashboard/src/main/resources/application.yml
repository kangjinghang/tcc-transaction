spring:
  application:
    name: @project.build.finalName@
  resources:
    chain:
      cache: false
server:
  servlet:
    context-path: /@project.build.finalName@
  port: 8888
zookeeper:
  connect-string: localhost:2181
